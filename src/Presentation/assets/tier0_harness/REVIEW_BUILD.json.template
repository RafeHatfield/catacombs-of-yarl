{
  "_what": [
    "TIER 0 REVIEW BUILD MARKER — ART-BIBLE-v0 §13.1.",
    "",
    "This file exists because of a gap the device control exposed: the review corridor was",
    "reachable only through --corridor-scene, and an iOS app receives no command line. The",
    "app could be verified installed on the reference device and still have no way to SHOW",
    "the corridor on it — which is most of the point. §13.1 requires the verdict to come from",
    "the lit scene at true display size on device, so the scene has to be bootable there.",
    "",
    "When a file named REVIEW_BUILD.json is present in this directory at boot AND no",
    "--corridor-scene flag was given, Main boots straight into the review corridor using the",
    "values below instead of the menu. Absent, the game boots normally — so a normal build is",
    "completely unaffected and this cannot leak into a player build by accident.",
    "",
    "This is a TEMPLATE. Copy it to REVIEW_BUILD.json to make a review build; the harness does",
    "that for you (tools/tier0_harness/build_review_app.sh). Install it under its own bundle id",
    "so it sits side by side with the real game rather than replacing it.",
    "",
    "THE LIGHT VALUES HERE ARE RULED FOR THE BOUNDARY — §6.2.1, Ruling 56 (Rafe, 2026-08-28),",
    "ratified by walking the tier-one floor family on the reference device at gameplay distance.",
    "They are copied from tools/tier0_harness/harness_config.yaml so the device build and the",
    "desktop capture are lit by the same rig; if they drift, the two stop being comparable.",
    "",
    "THE UNITS, because a knob position is not a value:",
    "  radiusTiles   reach in TILES, not pixels (32px tile -> 320px radius)",
    "  falloff       exponent on the radial ramp; 1.00 is the plain smoothstep, the identity",
    "  ambientLevel  SCALAR on the ambient hue, not a colour: 1a1a22 x 0.70 -> #121218",
    "",
    "RULED FOR THE BOUNDARY AND NOTHING ELSE. Every other region derives its own at its own",
    "gate; copying these into one would be conformance to a neighbouring region's answer."
  ],
  "_walk": [
    "WHICH WALK THIS BUILD IS FOR, and it is asserted before the build may install.",
    "",
    "Added 2026-09-05 after a build went to the handset for a rig-energy walk with NO ENERGY",
    "KNOB and the tier-0 STUB THEME under it - a magenta mock floor. Neither is visible to the",
    "frame critic, which judges the rendered frame and cannot see panel or scene wiring, so the",
    "critic passed correctly and the build was still unwalkable. That is a MECHANICAL gate and",
    "it lives in tools/tier0_harness/walk_precheck.py.",
    "",
    "  rig-energy   the §6.2.1 / Ruling 56 rig pass. Requires the energy control AND the real",
    "               floor family rendered - a rig walk that cannot reach the rig value, or that",
    "               shows the wrong surface, ratifies nothing.",
    "  surface      the tier-one surface gate. Requires both families composed and no mock.",
    "  none         not offered for a walk. Builds, does not assert.",
    "",
    "A walk that names requirements it does not carry REFUSES TO INSTALL and says which."
  ],
  "walk": "rig-energy",

  "_scene": [
    "THE COMBINED ROOM, not the tier-0 corridor junction, and this is the second half of the",
    "2026-09-05 defect. The corridor junction is a programmer-art scene lit through the STUB",
    "THEME: it draws the magenta placeholder where the floor family should be, which on the",
    "handset reads as a floor rendered in mock-wall colour. Every desktop frame the walk is",
    "compared against is tier1_combined_review.json with the real families laid.",
    "",
    "A walk build showing a different scene from the round it is meant to ratify is the wrong",
    "picture - the same reason TIER0_SCENE exists and the same reason its substitution is echoed."
  ],
  "scene": "res://src/Presentation/assets/tier0_harness/scenes/tier1_combined_review.json",
  "themeConfig": "res://src/Presentation/assets/tier1_ashlar/tile_themes_tier1_ashlar.yaml",

  "_families": [
    "EVERY ONE OF THESE IS LOAD-BEARING and the failure modes differ - the same list the desktop",
    "capture scripts carry, for the same reasons, because the device build must show the same",
    "room. Omit one and the scene does not fail: it draws something plausible and wrong.",
    "",
    "  ashlarFloor    omit and the floor is the MAGENTA placeholder",
    "  floorOverlays  omit and §12.1's plane-boundary occlusion silently disappears",
    "  boundaryWall   omit and the walls are the tier-0 MAGENTA MOCKS",
    "  wallBindings   omit and the walls are bare - §7.1 answered with nothing, silently",
    "  wallCap        omit and the walls have no top surface",
    "",
    "The engine reports which branch it took for each on every boot, so a device build missing",
    "one cannot be mistaken for a scene that simply has none (LOOP-PROCESS §4.2)."
  ],
  "ashlarFloor": "res://src/Presentation/assets/tier1_ashlar/MANIFEST.json",
  "boundaryWall": "res://src/Presentation/assets/tier1_walls/MANIFEST.json",
  "wallBindings": "res://src/Presentation/assets/tier1_bindings/MANIFEST.json",
  "wallCap": "res://src/Presentation/assets/tier1_cap/MANIFEST.json",

  "_voidRing": [
    "THE FLAT-DARK FALLBACK, matching every desktop capture of this room.",
    "",
    "§12.1a RULED the void dark by OCCLUSION and the wall manifest carries void_ring 0 for it,",
    "but that ruling's implementation is recorded-outstanding in the clause itself: at 0 the lamp",
    "lights 192 cells of solid rock and the room has no outside. Every capture the walk compares",
    "against therefore runs --void-ring 1 and says so in its log.",
    "",
    "Set here rather than left null because the handset has no command line, and a device room",
    "with no outside is not obviously wrong in the hand - which makes it worse than magenta, not",
    "better. The departure from a ruled manifest value is declared, and the engine echoes it."
  ],
  "voidRing": 1,
  "light": {
    "ambient": "1a1a22",
    "ambientLevel": 0.70,
    "color": "ffb066",
    "energy": 1.6,
    "radiusTiles": 5.0,
    "falloff": 1.00
  },

  "_floorOverlays": [
    "ART-BIBLE-v0 §8.3's incident overlays and §8.2.1's trodden channel, as a res:// path to a",
    "floor family's MANIFEST.json. Absent, the scene draws base tiles only — which is what every",
    "review build before tier one did, so an old marker keeps working unchanged.",
    "",
    "It is a separate key rather than part of themeConfig because an overlay is NOT A TILE ROLE.",
    "A cell may carry none, one or two of them, chosen per instance, and TileThemeLoader's schema",
    "has one id list per role and no shape for that. §8.3: a tile is the material, the incident",
    "is the variant.",
    "",
    "The engine reports which branch it took on every boot, so a device build that silently drew",
    "no incident at all cannot be mistaken for a floor that simply has none (LOOP-PROCESS §4.2)."
  ],
  "floorOverlays": "res://src/Presentation/assets/tier1_floors/MANIFEST.json",

  "_grid": [
    "Tile size and integer scale, mirroring --tile-size / --tile-scale. Present for the same",
    "reason the light values are: an iOS app receives no command line, so without these the",
    "device build would draw at the renderer's default 24 while the desktop captures it is",
    "meant to be compared against were taken at 32 — and nothing would have said so. The",
    "device and the capture must share the grid as well as the rig, or the §13.1 verdict is",
    "taken on the wrong picture.",
    "",
    "32 at x2 is RULED (Rafe, 2026-08-25), NOT derived — §4.3's derivation has not happened.",
    "Copied from tools/tier0_harness/harness_config.yaml; if they drift the two stop being",
    "comparable."
  ],
  "tileSize": 32,
  "tileScale": 2.0,

  "_reviewStatus": [
    "WHAT THIS BUILD WAS ALLOWED PAST. Stamped by build_review_app.sh, never authored here.",
    "",
    "null            the build carries a frame-critic PASS for its exact build id.",
    "SKIPPED-REVIEW  YARL_SKIP_CRITIC=1 waved it past the gate. Legitimate for producing a",
    "                build to MEASURE; never a build to take a verdict on.",
    "",
    "The app draws this in the corner of the screen and reports it in its BUILD IDENTITY line,",
    "because the two kinds of build are indistinguishable in the hand and the hazard is walking a",
    "measurement build as a gate build. An override nobody can see from the phone is the same as",
    "no gate at all."
  ],
  "reviewStatus": null
}
