# GENERATED by tools/floor_remediation/capture_children.py - do not hand-edit.
# Walls: the composition spike's `before` arm, UNCHANGED and held constant.
# Floor: S_S_seed9803 (tile 9402) in every floor slot. The floor is the only variable.
# THE COMPOSITION SPIKE. Composed wall segments + MOCK binding overlays. NOT ART, NOT A PALETTE,
# NOT A CANDIDATE. ART-BIBLE-v0 §13.1 governs landing; nothing here is offered for it.
tile_root: "res://src/Presentation/assets/floor_remediation"
tile_pattern: "fr_{id}.png"

themes:
  sandstone:
    floor_primary: [9402]
    floor_accent: [9402]
    floor_dark: [9402]
    floor_interior: [9402]
    floor_worn: [9402]
    wall_autotile:
      0: [9200]   # top band + front face (floor below)
      1: [9209]   # top band + front face (floor below)
      2: [9218]   # top band + front face (floor below)
      3: [9227, 9228, 9229, 9230, 9231, 9232, 9233, 9234, 9235]   # top band + front face (floor below)
      4: [9236]   # top surface (wall below)
      5: [9245]   # top surface (wall below)
      6: [9254]   # top surface (wall below)
      7: [9263]   # top surface (wall below)
      8: [9272]   # top band + front face (floor below)
      9: [9281]   # top band + front face (floor below)
      10: [9290]   # top band + front face (floor below)
      11: [9299]   # top band + front face (floor below)
      12: [9308, 9309, 9310, 9311, 9312, 9313, 9314, 9315, 9316]   # top surface (wall below)
      13: [9317]   # top surface (wall below)
      14: [9326]   # top surface (wall below)
      15: [9335, 9336, 9337, 9338, 9339, 9340, 9341, 9342, 9343]   # top surface (wall below)
    wall_diagonal:
      corner_outer_nw: [9280]
      corner_outer_ne: [9281]
      corner_outer_sw: [9282]
      corner_outer_se: [9283]
      # ROUND 3: a LIST. interior_fill is 267 of the ~300 wall cells in this corridor, so a
      # scalar here stamped one tile across nearly the whole solid mass while the mask lists
      # varied the visible 6%. The seat culled on it twice before the cause was found.
      interior_fill: [9335, 9336, 9337, 9338, 9339, 9340, 9341, 9342, 9343]
    stair_down: [9140]
    stair_up: [9141]

default_theme: sandstone
