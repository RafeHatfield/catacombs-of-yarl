# Tech Debt — Tidy-Later (from Weighing code review, 2026-06-06)

## Status: open (tidy-later — none blocking)

Low-risk items surfaced during the pre-balance-pass review of the Weighing endgame.
Decision (Rafe): file as tidy-later, batch when next in the relevant files — no dedicated pass.
The review's blocking items (#1 headless soft-lock, #3 per-run excess) were fixed separately.

---

- [~] DEBT-014: `UnderWardenData.CumulativeUnprovokedKills` is now write-only
  - DECISION (Rafe, 2026-06-06): **KEEP, inert. Revisit only during a future persistence-cleanup pass.**
  - Files: `src/Logic/Persistence/Namespaces/UnderWardenData.cs` (field now documents the intent),
    flush at `src/Presentation/Main.cs` ~1645
  - Context: the #3 fix switched the audit (Auditor's Own + Oathkeeper fine scaling) to read THIS
    descent's kills from `RunAggressionTally`. The lifetime cumulative is still flushed but unread.
  - Rationale: asymmetric risk — keeping costs nothing (write-only, harmless); removing costs a
    migration. And it's a thematically rich seam: a future Under-Warden career-cruelty memo/audit
    line (the institution noting the whole record even on a clean run — "across all descents, ended
    a great many lives that posed no threat"), mirroring the `WeighingRefusals` lifetime-flag pattern.
  - Remove only if a future cleanup pass coincides with a decision to never write career-cruelty content.

- [ ] DEBT-015: Three parallel `GuardianId` switch tables in `WeighingOrchestrator`
  - File: `src/Logic/Endgame/WeighingOrchestrator.cs` — `GuardianName`, `GuardianTypeId`, `StatsFor`
    (and `GuardianFallbackKey`)
  - Adding/renaming a Guardian means editing all in lockstep. Collapse to a single table-of-record
    keyed on `GuardianId`. Closed set of 5 today, so low urgency — most worth doing of this batch.

- [ ] DEBT-016: `WeighingOrchestrator.NextId` is a hand-rolled scan-for-max allocator
  - File: `src/Logic/Endgame/WeighingOrchestrator.cs` `NextId`
  - The project already has `EntityFactory.NextId` / `EntityIdAllocator` (see DEBT-008, dual ID
    systems). Works here (only the orchestrator mutates the arena), but it's a third allocator.
    Fold into the unification when DEBT-008 is addressed.

- [ ] DEBT-017: `WeighingArena.Anchors` leaks mutable lists
  - File: `src/Logic/Endgame/WeighingArena.cs`
  - Exposed as `IReadOnlyDictionary<string, List<...>>` but the `List` values stay mutable — callers
    can mutate anchor lists. `AnchorsFor`/`FirstAnchor` are the intended API; drop the raw property or
    wrap the values.

- [ ] DEBT-018: `PossessionSystem.CheckVisibilityConstraint` null-guard readability
  - File: `src/Logic/Core/PossessionSystem.cs` ~135
  - `if (!home.Get<Fighter>()?.IsAlive == true) return;` is correct but hard to parse and silently
    no-ops on a null Fighter. `if (home.Get<Fighter>() is { IsAlive: false }) return;` reads cleaner.

- [ ] DEBT-019: `wraith`/`lich` entries in `possession_enter.undead` pool are dead data
  - File: `config/voice_lines/possession.yaml` (undead enter pool) — both are possession-immune, so
    the lines are unreachable. Harmless; one-line cleanup whenever next in the file.
