# Tech Debt & Best Practices — Audit 2026-03-30

## Status: complete

Findings from deep codebase audit. 732 tests passing at time of audit. Architecture boundary clean (zero Godot imports in logic layer). Issues ordered by priority.

---

## CRITICAL — Correctness Bugs

- [x] DEBT-001: `dragon_fart` spell applies `SilencedEffect` but emits `statusName: "sleep"`
  - File: `src/Logic/Combat/SpellResolver.cs` ~line 87-89
  - Fix: Change to apply `SleepEffect` (matches "stupor" flavor text and statusName)
  - Status: complete
  - Files changed: `src/Logic/Combat/SpellResolver.cs`
  - Notes: Changed `ResolveStatusEffect<SilencedEffect>` → `ResolveStatusEffect<SleepEffect>` and updated `applyEffect` lambda accordingly. No existing tests directly tested the applied effect type (only YAML loading); no test update needed.

- [x] DEBT-002: `PlayerCarryForward.Apply()` drops LeftRing, RightRing, Neck on floor descent
  - File: `src/Logic/Core/PlayerCarryForward.cs` lines 56-62
  - Fix: Copy the 3 missing slots
  - Status: complete
  - Files changed: `src/Logic/Core/PlayerCarryForward.cs`, `tests/Core/PlayerCarryForwardTests.cs`
  - Notes: Added the 3 missing slot copies. Added `Apply_AllEightEquipmentSlots_SurviveFloorTransition` test covering all 8 slots.

---

## HIGH — Tech Debt

- [x] DEBT-003: `StatusEffectProcessor.RemoveEffect` manual dispatch switch (23 cases)
  - File: `src/Logic/Combat/StatusEffects/StatusEffectProcessor.cs` lines 253-283
  - Fix: Add `Entity.RemoveByType(Type t)` method; collapse switch to one-liner
  - Status: complete
  - Files changed: `src/Logic/ECS/Entity.cs`, `src/Logic/Combat/StatusEffects/StatusEffectProcessor.cs`
  - Notes: Added `RemoveByType(Type t)` to Entity; `Remove<T>()` now delegates to it. `RemoveEffect` is now a one-liner calling `entity.RemoveByType(effect.GetType())`. All 732 existing tests still pass — no behavioral change.

- [x] DEBT-004: `scroll_of_raise_dead` in floor item pool but handler is a no-op
  - File: `config/entities.yaml` line ~529
  - Fix: Set weight to 0 until corpse system exists
  - Status: complete
  - Files changed: `config/entities.yaml`
  - Notes: Changed `weight: 4` → `weight: 0` with comment explaining why. The definition stays so plan_monster_specials can enable it without YAML archaeology.

- [x] DEBT-005: `DecideFlee` mixes Chebyshev baseline with Manhattan candidate evaluation
  - File: `src/Logic/AI/BasicMonsterAI.cs` lines 259-289
  - Fix: Use consistent metric throughout (Manhattan everywhere for flee)
  - Status: complete
  - Files changed: `src/Logic/AI/BasicMonsterAI.cs`
  - Notes: Changed `monster.ChebyshevDistanceTo(...)` baseline to `Math.Abs(dx) + Math.Abs(dy)` Manhattan. Previously the baseline was always ≤ the candidate evaluation, making "no improvement" cases slightly different — now both sides use the same ruler. Behavioral effect is minimal (monsters were rarely cornered in a way where this diverged), but the logic is now correct.

- [x] DEBT-006: Dead field `_lastAllocBytes` in GameController
  - File: `src/Presentation/GameController.cs` line 501
  - Fix: Remove
  - Status: complete
  - Files changed: `src/Presentation/GameController.cs`
  - Notes: Removed the debug comment and dead field declaration together.

---

## MEDIUM — Tech Debt (deferred, needs separate design work)

- [ ] DEBT-007: `AliveMonsters` cache can serve stale data within same turn
  - File: `src/Logic/Core/GameState.cs` lines 108-119
  - Fix: Add `InvalidateMonsterCache()` method, call when monster dies
  - Note: partially mitigated by `IsAlive` checks in TurnController

- [ ] DEBT-008: Dual entity ID systems (`EntityFactory._nextId` + `EntityIdAllocator`)
  - Files: `src/Logic/ECS/EntityFactory.cs`, `src/Logic/Core/EntityIdAllocator.cs`
  - Fix: Unify into single ID source — complex, needs careful design

- [ ] DEBT-009: `ContentLoader.Merge` uses default-value sentinels for inheritance detection
  - File: `src/Logic/Content/ContentLoader.cs` lines 317-375
  - Fix: Use nullable types for mergeable fields so null vs explicit-default are distinguishable
  - Risk: data integrity for child definitions overriding back to default values

---

## MISSING TESTS

- [x] DEBT-010: `StatusEffectProcessor.ClearAllEffects` — no test verifying all 23 effect types removed
  - Status: complete
  - Files changed: `tests/Core/StatusEffectLifecycleTests.cs`
  - Notes: Added `ClearAllEffects_RemovesAllEffectTypes` applying 12 different effect types (covering DOT, HOT, skip-turn, buff, debuff categories) and asserting zero IStatusEffect components remain after ClearAllEffects. This test also serves as a regression guard for DEBT-003 — if RemoveByType breaks for any type, this test will catch it.

- [ ] DEBT-011: `ContentLoader.Merge` sentinel edge case — child setting field back to parent default
  - Add test once DEBT-009 is resolved

- [ ] DEBT-012: `ResolveCorrosion` unit-level edge cases (metal check, DamageMin==DamageMax guard)
  - Add targeted unit tests separate from scenario YAML

---

## CLEANUP

- [x] DEBT-013: `orc_grunt` in entities.yaml — spawn_weight 0, no depth_weights, appears to be legacy test artifact
  - Status: complete (no change needed)
  - Notes: `orc_grunt` is referenced as a spawn type in 20+ scenario YAML files and `config/level_templates_testing.yaml`. The definition must stay. It has `spawn_weight: 0` already (correct — it cannot drop procedurally, it is only placed explicitly by scenarios).
